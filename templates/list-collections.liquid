{% paginate collections by 4 %}    

<h1> Collection </h1>

{% for coll in collections %}
 
  <a href ="{{coll.url }}">
   <h1> {{coll.title}} </h1> 
   <p>Total Collection : {{coll.all_products_count}} </p>
  </a>
{% endfor %}


{{ paginate | default_pagination}}
{% endpaginate %}